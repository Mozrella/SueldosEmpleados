@model Empleados_TFI_PAD.departamento

@{
    ViewBag.Title = "Borrar Depto";
}

<h2>Borrar</h2>

<h3>Realmente deseas eliminar esto?</h3>
<div>
    <h4>Departamento</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @*@Html.DisplayNameFor(model => model.nombre_depto)*@
            Nombre
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre_depto)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-group">
            <div class="form-actions no-color col-md-offset-2 col-md-10">
                <input type="submit" value="Borrar" class="btn btn-danger" />
            </div>
        </div>

        @*<div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>*@

        <div>
            @Html.ActionLink("Volver", "Index")
        </div>
    }
</div>
