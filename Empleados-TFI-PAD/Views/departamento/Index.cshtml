@model IEnumerable<Empleados_TFI_PAD.departamento>

@{
    ViewBag.Title = "Departamentos";
}

<h2>Departamentos</h2>

<p>
    @Html.ActionLink("Nuevo +", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.nombre_depto)*@
            Departamento
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_depto)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_depto }) |
            @Html.ActionLink("Ver", "Details", new { id=item.id_depto }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.id_depto })
        </td>
    </tr>
}

</table>
